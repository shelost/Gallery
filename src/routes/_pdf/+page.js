// This tells SvelteKit to only render this page on the client side
export const ssr = false;

// This ensures the page is generated at build time (if you build a static site)
export const prerender = false;